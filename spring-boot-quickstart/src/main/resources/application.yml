spring:
    application:
        name: spring-boot-quickstart

server:
    contextPath: /springboot-greet
    port: 9090
    jetty:
        accesslog:
            enabled: true
    compression:
        enabled: true
        mimeTypes: text/xml, text/css, text/html, application/json
        minResponseSize: 4096